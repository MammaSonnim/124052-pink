mixin logo(isImg)
  - var href = isIndex ? false : "index.html"
  - var className = isIndex ? "text" : "link"

  +b.logo
    +e.A(class="__" + className, href=href) Pink
      +e.SVG.icon-img
        use
